# Hugo configuration file
title: Aerith Netzer

# import hextra as module
module:
  imports:
  - path: github.com/imfing/hextra

markup:
  # allow raw html
  goldmark:
    renderer:
      unsafe: true

  # enable hextra syntax highlight
  highlight:
    noClasses: false

menu:
  main:
    - name: About
      pageRef: /about
      weight: 10
    - name: Blog
      pageRef: /blog
      weight: 11
    - name: Contact ↗
      url: "mailto:aerith.netzer@northwestern.edu"
      weight: 20
    - name: Search
      weight: 30
      params:
        type: search
    - name: GitHub
      weight: 40
      url: "https://github.com/aerithnetzer/aerithnetzer.github.io"
      params:
        icon: github

params:
  navbar:
    displayTitle: true
    displayLogo: false

  footer:
    displayCopyright: false
    displayPoweredBy: true

  editURL:
    enable: true
    base: "https://github.com/aerithnetzer/aerithnetzer.github.io/edit/main/content"
